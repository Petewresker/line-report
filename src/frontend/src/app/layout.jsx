import "./globals.css";

import "./globals.css";

export const metadata = {
  title: "Line Report",
};

export default function RootLayout({ children }) {
  return (
    <html lang="th">
      <body>{children}</body>
    </html>
  );
}
